Для запуска приложения необходимо использовать jee-контейнер glassfish.

разработано серверное приложение с поддержкой rest api, которое позволяет при переходе на определённые ресурсы(см.ниже) получать ответы в формате JSON.

Запросы:

http://localhost:8080/example1/api/agents/providerInfoList (позволяет получить список всех данных по реквизитам(расчетный счет, ИНН, КПП)всех поставщиков);
http://localhost:8080/example1/api/agents/bankBaseInfoList (позволяет получить список всех данных по реквизитам банков(имя, ИНН, корсчет, БИК, адрес, признак акутальности));
http://localhost:8080/example1/api/agents/agentId/providerInfo/providers/providerId , где {agentId} - уникальный ID agent, {providerId} - уникальный ID поставщика (запрос позволяет получить список данных по реквизитам(расчетный счет, ИНН, КПП) поставщика);
http://localhost:8080/example1/api/agents/agentId/bankBaseInfo/banks/bankId , где {agentId} - уникальный ID agent, {bankId} - уникальный ID bank (запрос позволяет получить список данных по реквизитам банка(имя, ИНН, корсчет, БИК, адрес, признак акутальности);
